import { IGitRoute } from '../helpers/types'

export const gitRoutes: IGitRoute[] = [
    { route: 'stash', title: 'Stash' },
    { route: 'clean_up_remote_branch', title: 'Clean up remote branch' },
]

